# Eazo Private Key to decrypt the user session token on the server
EAZO_PRIVATE_KEY=your_eazo_private_key_here

# Eazo app ID. Read by RootLayout and passed into <EazoProvider> as a prop.
EAZO_APP_ID=your_eazo_app_id_here

# App-internal AI billing. Default mode calls Creator's proxy so Eazo official
# model usage is charged to the app creator's credits. Set BYOK values only
# when the creator chooses their own provider/key.
EAZO_APP_AI_API_BASE=https://eazo.ai/creator
EAZO_AI_PROVIDER_MODE=eazo
EAZO_AI_MODELS_JSON={"text":"deepseek.v3.1","image_generation":"gemini_31_flash_image","speech_to_text":"whisper_1","text_to_speech":"gemini_31_flash_tts_preview"}
EAZO_AI_MODEL_KEY=deepseek.v3.1
AI_PROVIDER_BASE_URL=
AI_PROVIDER_API_KEY=
AI_PROVIDER_MODEL=

# Product title/description shown as the app's <title> and meta description
# (consumed by src/app/layout.tsx). The platform stamps these at scaffold
# time; layout.tsx falls back to "Eazo App" / "An app build by eazo.ai" when
# unset.
NEXT_PUBLIC_APP_TITLE=Eazo App
NEXT_PUBLIC_APP_DESCRIPTION=An app build by eazo.ai

# PostgreSQL connection string (used by Drizzle ORM)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/myapp

# Shared secret Vercel Cron uses to authenticate scheduled invocations of
# `/api/notifications/cron/daily-digest`. Generate any high-entropy string
# (e.g. `openssl rand -hex 32`) and set it both here and in Vercel's project
# env. Vercel automatically injects `Authorization: Bearer ${CRON_SECRET}`
# when firing crons declared in `vercel.json`.
CRON_SECRET=replace_with_a_long_random_string
